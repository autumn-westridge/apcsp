function encrypt(plainText) {
    // return the encrypted text
}

function decrypt(encryptedText) {
    // return the decrypted text
}

var testStr = "Hello world";
var testStrEncrypted = encrypt(testStr);
console.log(testStrEncrypted); // should not log Hello World
console.log(decrypt(testStrEncrypted)); // should log Hello World
